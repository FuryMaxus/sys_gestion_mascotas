spring.application.name=servicio-gestion
spring.mvc.problemdetails.enabled=true
server.port=${APP_PORT:8081}
jwt.secret=${JWT_SECRET:secret_key}
spring.jpa.hibernate.ddl-auto=${DB_DDL_STRATEGY:create-drop}

